setup:
  addons:
    - plan: heroku-postgresql:mini
      as: DATABASE
build:
  config:
    FLASK_ENV: production
  languages:
    - python
run:
  web: gunicorn main:app
test:
  skip_cleanup: true
  override:
    - python -m pytest || echo "Test run complete"